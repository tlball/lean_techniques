# frozen_string_literal: true

source 'https://rubygems.org'

# Using this instead of a .ruby-version so I don't force a specific ruby install for this project
ruby "~> 3.0"

group :test do
  gem 'pry-byebug'
  gem 'rspec'
end
